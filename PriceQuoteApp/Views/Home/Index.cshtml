@model PriceQuoteApp.Models.PriceQuotation

@{
    ViewData["Title"] = "Price Quotation";
}

<!DOCTYPE html>
<html>
<head>
    <title>Price Quotation</title>
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Price Quotation</h1>
        <form method="post" class="needs-validation" novalidate>
            <div class="form-group row">
                <label for="Subtotal" class="col-sm-2 col-form-label">Subtotal:</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control w-25" id="Subtotal" name="Subtotal" value="@Model.Subtotal" required>
                    <span asp-validation-for="Subtotal" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group row">
                <label for="DiscountPercent" class="col-sm-2 col-form-label">Discount Percent:</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control w-25" id="DiscountPercent" name="DiscountPercent" value="@Model.DiscountPercent" required>
                    <span asp-validation-for="DiscountPercent" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group row">
                <label for="DiscountAmount" class="col-sm-2 col-form-label">Discount Amount:</label>
                <div class="col-sm-10">
                    <p class="form-control-plaintext" id="DiscountAmount">@Model.DiscountAmount.ToString("C")</p>
                </div>
            </div>
            <div class="form-group row">
                <label for="Total" class="col-sm-2 col-form-label">Total:</label>
                <div class="col-sm-10">
                    <p class="form-control-plaintext" id="Total">@Model.Total.ToString("C")</p>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-sm-10">
                    <button type="submit" class="btn btn-primary">Calculate</button>
                    <a href="/Home/Reset" class="btn btn-secondary ml-2">Clear</a>
                </div>
            </div>
        </form>

        <div>
            <ul>
                @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                {
                    <li class="text-danger">@error.ErrorMessage</li>
                }
            </ul>
        </div>
    </div>

    <script src="https://ajax.aspnetcdn.com/ajax/jquery/jquery-3.5.1.min.js"></script>
    <script src="https://ajax.aspnetcdn.com/ajax/jquery.validate/1.19.2/jquery.validate.min.js"></script>
    <script src="https://ajax.aspnetcdn.com/ajax/mvc/5.2.7/jquery.validate.unobtrusive.min.js"></script>
    <script>
        function clearFields() {
            // Reset the form to its initial state
            document.querySelector('form').reset();

            // Clear validation messages
            $('form').validate().resetForm();
            $('form').find('.text-danger').removeClass('text-danger');
        }
    </script>
</body>
</html>
